import * as React from 'react';
import { HomePageWrapper } from './HomePageStyles';

interface Props {}

const HomePage: React.SFC<Props> = (props: Props) => {
  return (
    <HomePageWrapper>HomePage Component</HomePageWrapper>
  );
};

export default HomePage;
